#include <vector>
#include "d2Maze.h"


class MazeCompression{
public:

    MazeCompression(){};
    ~MazeCompression(){};

    vector<int>comprress(vector<int> arr);
    vector<int>deComprress(vector<int> arr);

};
